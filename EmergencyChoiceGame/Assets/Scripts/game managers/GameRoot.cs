using UnityEngine;

public class GameRoot : MonoBehaviour
{
    public static GameRoot Instance;


    void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
